// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
      extend: {
        fontFamily: {
          // Aqui definimos a chave "roboto" que poderá ser usada via "font-roboto" nas classes do Tailwind
          unbounded: ['Unbounded', 'normal'],
        },
      },
    },
    plugins: [],
  }
  